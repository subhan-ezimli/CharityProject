

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
	@using WebApp.Views.ViewModels;
	@model RejectViewModel

	<!DOCTYPE html>
	<html lang="en">

	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Payment2</title>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
		<style>
			.timer {
				display: flex;
				align-items: end;
				justify-content: start;
				height: 100%;
				width: 100%;
			}
		</style>
	</head>

	<body style="
		  	display: flex;
		  	justify-content: center;
		  	align-items: center;
		  	height: 100vh;
		  	background: #f0f0f0;
			">
		<div style="
				width: 400px;
				background-color: #ffffff;
				padding-top: 40px;
				padding-bottom: 40px;
				padding-left: 30px;
				padding-right: 30px;
				border-radius: 6px;
				border: 1px solid var(--borderColor);
				box-shadow: 0 10px 20px -2px rgba(0, 0, 0, 0.1),
			  	0 4px 15px -2px rgba(0, 0, 0, 0.01);
			  ">
			<div class="timer">
				<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1"
					 viewBox="0 0 300 300" preserveAspectRatio="none" style="width:100; height:100; top:0; left:0;">
					<circle cx="100" cy="100" r="57" id="pink-halo" fill="none" stroke="#c02d2d" stroke-width="10"
							stroke-dasharray="361, 0" transform="rotate(-90,100,100)" />
					<text id="myTimer" text-anchor="middle" x="100" y="110" style="font-size: 40px; fill:#2d2c2c;"></text>
				</svg>
			</div>
			<div style="
			  	display: flex;
			  	justify-content: center;
			  	align-items: center;
			  	flex-direction: column;
				">
				<div style="
					border: 3px solid #c02d2d;
					border-radius: 50%;
					width: 100px;
					height: 100px;
					display: flex;
					align-items: center;
					justify-content: center;
				  ">
					<i class="fa-solid fa-xmark" style="
				  	color: #c02d2d;
				  	font-size: 4rem;
				  	padding-top: 1rem;
				  	padding-bottom: 1rem;
					"></i>
				</div>
				<h2 style="color: #c02d2d; font-size: 28px">Ödəniş edilmədi!</h2>
			</div>
			<p style="color: #4a4a4a; font-weight: 500; text-align: center">
				@Model.ErrorMessage
			</p>
		</div>
		<script>
			(function () {
				var circle = document.getElementById('pink-halo');
				var myTimer = document.getElementById('myTimer');
				var t = 5;
				var interval = 30;
				var angle = 0;
				var angle_increment = 360 / t;
				var intervalCounter = 0;

				window.timer = window.setInterval(function () {
					intervalCounter++;
					circle.setAttribute("stroke-dasharray", angle + ", 361");

					myTimer.innerHTML = t - parseInt((angle / 360) * t);

					if (angle >= 360) {
						window.clearInterval(window.timer);
						window.close()
					}

					angle += angle_increment / (1000 / interval);

				}.bind(this), interval);
			})()
		</script>
	</body>

	</html>
}
